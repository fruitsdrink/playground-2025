---
import { BaseLayout } from "@/layouts";
import { getCollection } from "astro:content";

const teams = await getCollection("team");
---

<BaseLayout title="集合-json">
  <div>
    <pre>{JSON.stringify(teams, null, 2)}</pre>
  </div>
</BaseLayout>
